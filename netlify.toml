[build]
  base    = "app"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# React Router usa BrowserRouter — cualquier ruta bajo /app/* sirve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
